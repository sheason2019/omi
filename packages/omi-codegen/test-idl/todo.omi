struct Todo {
  int id;
  string content;
  boolean finish;
  int createTime;
}

// block
service Todo {
  repeated Todo GetTodoList();
  void PostTodo(string content);
  void PutTodo(Todo todo);
  void DeleteTodo(int id);
}