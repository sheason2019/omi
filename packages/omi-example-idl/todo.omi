// block
struct Todo {
  // variable block
  optional repeated string content; // variable inline
  boolean finish;
  int createTime;
}

// block
service Todo { // function inline
  // function block
  optional repeated Todo GetTodoList();
  void PostTodo(Todo todo);
  void PutTodo(Todo todo);
  void DeleteTodo(Todo todo);
}